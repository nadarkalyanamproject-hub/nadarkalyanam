NEXT_PUBLIC_API_URL="http://localhost:4000/api/v1"
# Same API the member web app uses — in production, set to the deployed
# API's URL (e.g. https://your-api.onrender.com/api/v1). NEXT_PUBLIC_* vars
# are baked into the client bundle at build time — set this before running
# `next build`.
